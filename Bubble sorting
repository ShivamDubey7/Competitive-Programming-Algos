def bubbleSort(a):
    
 
  for i in range(len(a)):

    
    for j in range(0, len(a) - i - 1):

     
      if a[j] > a[j + 1]:

        temp = a[j]
        a[j] = a[j+1]
        a[j+1] = temp


data = [-44, 45, 0, 85, -30]

bubbleSort(data)

print('Sorted Array in Ascending Order:')
print(data)
